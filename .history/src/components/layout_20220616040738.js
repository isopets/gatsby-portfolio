import * as React from "react"
import Header from "./header"
import Footer from "./footer"

const Layout = (props) => {
  return (
    <>
      <Header />
      <main>{prop}</main>
      <Footer />
    </>
  )
}

export default Layout
